source "https://rubygems.org"

# Installs gems under vendor/bundle (see .bundle/config) so sudo is not required.
# On Debian/Ubuntu/WSL, native gems need: sudo apt install ruby-dev build-essential
gem "github-pages", group: :jekyll_plugins
gem "jekyll-include-cache"
